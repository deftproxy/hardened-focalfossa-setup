to fix issue where hostname is set to 'localhost', reboot
to fix issue where there is no IP address, do the following: 
edit 00.... in this directory, change the ip details to the correct settings, save and close (recommend using nano)
example command: nano 

run the following command:
cp 00.... etc/netplan/
